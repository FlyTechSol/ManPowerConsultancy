namespace MC.Domain.Entity.Enum.Registration
{
    public enum VerificationStatus
    {
        Pending, 
        InProgress, 
        Completed, 
        Failed, 
        Incomplete, 
        OnHold, 
        Other
    }
}
